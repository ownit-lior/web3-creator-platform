/** Chain ids used by the app — mirrors wagmi/chains without requiring wagmi install. */
export const baseSepolia = { id: 84532, name: 'Base Sepolia' } as const
export const base = { id: 8453, name: 'Base' } as const
